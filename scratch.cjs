const fs = require('fs'); const file = 'app/page.tsx'; const lines = fs.readFileSync(file, 'utf8').split('\n'); const beforePricing = lines.slice(0, 362); const pricingSection = lines.slice(362, 416); const betweenSections = lines.slice(416, 417); const contactSection = lines.slice(417, 479); const afterContact = lines.slice(479); const newLines = [...beforePricing, ...contactSection, ...betweenSections, ...pricingSection, ...afterContact]; fs.writeFileSync(file, newLines.join('\n')); console.log('Swapped successfully');
